# @package _global_
defaults:
  - model: ogbg_molpcba

training:
  lr: 0.002
  epochs: 100
  warmup: 5
  weight_decay: 0.0
  batch_size: 512

random_walk:
  length: 25
  window_size: 8
  sample_rate: 0.5

model:
  node_embed: true
  edge_embed: true
  dropout: 0.4
  global_pool: mean
