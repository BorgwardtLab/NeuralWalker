# @package _global_
defaults:
  - model: mamba+vn_cluster

training:
  lr: 0.01
  epochs: 100
  warmup: 5
  weight_decay: 0.0
  batch_size: 32

random_walk:
  length: 200
  window_size: 32
  sample_rate: 0.5

model:
  global_pool: null
